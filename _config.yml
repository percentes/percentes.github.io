title: Percentes
description: >-
  Reliability measurement for LLM inference under load and failure.
  Every scheduled request is accounted for, and the measuring client
  must pass its own gates before any number counts.
url: "https://percentes.ai"
baseurl: ""
author: Varun Mahadkar

markdown: kramdown
permalink: /writing/:year/:title/
plugins:
  - jekyll-feed
  - jekyll-sitemap

feed:
  path: feed.xml

# Keep Jekyll out of files it should not touch.
exclude:
  - README.md
  - SITE-DEPLOY.md
